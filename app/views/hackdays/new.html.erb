<h1>Create A New Hackday</h1>
<div class "row">
	<div class="span6 offset3">
		<%= form_for(@hackday) do |f| %>

			<%= f.label :name %>
			<%= f.text_field :name %>

			<%= f.submit "Create new hackday!", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>

